database:
  override:
    - echo "Nothing to do here"

machine:
  ruby:
    version: 2.3.0

notify:
  webhooks:
    - url: https://xrayci.kap.la/webhooks/builds

test:
  override:
    - bundle exec rake

